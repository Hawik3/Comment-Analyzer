<Window
    x:Class="Comment_Analyzer.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Comment_Analyzer.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Comment_Analyzer.ViewModel"
    Title="MainWindow"
    Width="1080"
    Height="480"
    mc:Ignorable="d">
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Offset="0" Color="LightGray" />
            <GradientStop Offset="1" Color="WhiteSmoke" />
        </LinearGradientBrush>
    </Window.Background>
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <StackPanel>

        <StackPanel Margin="25,0,0,0" Orientation="Horizontal">
            <Button
                Width="60"
                Height="30"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Background="White"
                Command="{Binding OpenCommand}" Content="Open"/>

            <StackPanel Margin="10,7,10,0">
                <TextBlock Text="Comment text column:" />
                <ComboBox
                    Background="White"
                    ItemsSource="{Binding Numbers}"
                    SelectedValue="{Binding CommentTextColumn}" />

            </StackPanel>
            <StackPanel Margin="10,7,10,0">
                <TextBlock Text="Date time column:"  />
                <ComboBox
                    Background="White"
                    ItemsSource="{Binding Numbers}"
                    SelectedValue="{Binding DateTimeColumn}" />

            </StackPanel>
            <TextBlock
                VerticalAlignment="Bottom"
                FontSize="20"
                Text="{Binding FilePath}" />
        </StackPanel>
<<<<<<< Updated upstream
        <StackPanel Margin="25,1,0,0" Orientation="Horizontal">
            <TextBlock Text="Sentiment average score: " />
            <TextBlock Text="{Binding Score}" />
        </StackPanel>


        <ScrollViewer
            Height="300"
            Margin="25,0,25,0"
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Visible">
            <DataGrid
                Name="MetricsDataGrid"
                AutoGenerateColumns="False"
                EnableRowVirtualization="True"
                IsReadOnly="True"
                ItemsSource="{Binding Scores}"
                VirtualizingPanel.IsVirtualizing="True">

                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="800"
                        Binding="{Binding CommentText}"
                        Header="Comment text" />
                    <DataGridTextColumn
                        Width="180"
                        Binding="{Binding Score}"
                        Header="Prediction score" />
                </DataGrid.Columns>
=======
        <TabControl
            x:Name="tabs"
            Margin="20,20,20,20"
            SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem>
                <TabItem.Header>
                    <TextBlock Text="Start page" />
                </TabItem.Header>
                <TabItem.Content>
                    <StackPanel>
                        <TextBlock Text="Comment Analyzer v0.2" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Select excel file with comments and select column with data." FontSize="16" />
                        <TextBlock FontSize="16" >Comment text often have header "TextDisplay" or "TextOriginal". Better to choose "TextOriginal" because "TextDisplay" have tags</TextBlock>
                        <TextBlock FontSize="16" >Column with data often have header "PublishedAtUTC" or "UpdatedAtUTC". Better to choose "PublishedAtUTC"</TextBlock>
                        <TextBlock FontSize="20" FontWeight="Bold"> Don't forget to delete headers to avoid bugs!</TextBlock>
                    </StackPanel>
                </TabItem.Content>
            </TabItem>
            <TabItem IsEnabled="{Binding IsFileSelected}">
                <TabItem.Header>
                    <TextBlock>Sentiment</TextBlock>
                </TabItem.Header>
                <TabItem.Content>
                    <StackPanel>
                        <StackPanel Margin="25,1,0,0" Orientation="Horizontal">
                            <TextBlock Text="Sentiment average score: " />
                            <TextBlock Text="{Binding AverageSentimentScore}" />
                        </StackPanel>


                        <ScrollViewer
                            Height="320"
                            Margin="25,0,20,10"
                            HorizontalScrollBarVisibility="Auto"
                            VerticalScrollBarVisibility="Visible">
                            <DataGrid
                                Name="MetricsDataGrid"
                                AutoGenerateColumns="False"
                                EnableRowVirtualization="True"
                                IsReadOnly="True"
                                ItemsSource="{Binding SentimentScores}"
                                ScrollViewer.CanContentScroll="True"
                                VirtualizingPanel.IsVirtualizing="True">

                                <DataGrid.Columns>
                                    <DataGridTextColumn
                                        Width="750"
                                        Binding="{Binding CommentText}"
                                        Header="Comment text"
                                        ScrollViewer.CanContentScroll="True"
                                        VirtualizingPanel.IsVirtualizing="True" />
                                    <DataGridTextColumn
                                        Width="180"
                                        Binding="{Binding Score}"
                                        Header="Prediction score"
                                        ScrollViewer.CanContentScroll="True"
                                        VirtualizingPanel.IsVirtualizing="True" />
                                </DataGrid.Columns>

                            </DataGrid>
                        </ScrollViewer>
                    </StackPanel>
                </TabItem.Content>
            </TabItem>
            <TabItem IsEnabled="{Binding IsFileSelected}">
                <TabItem.Header>
                   Timeline
                </TabItem.Header>
                <TabItem.Content>
                    <StackPanel>

                        <lvc:CartesianChart
                            Height="300"
                            Series="{Binding CommentTimeline}"
                            XAxes="{Binding XAxis}"
                            ZoomMode="X" />

                    </StackPanel>
                </TabItem.Content>
            </TabItem>
        </TabControl>
>>>>>>> Stashed changes

            </DataGrid>
        </ScrollViewer>
    </StackPanel>
</Window>
